[project]
name = 'counter'
description = 'Small counter program'
authors = ["Yanuka Deneth"]
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.counter]
path = 'contracts/counter.clar'
clarity_version = 1
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
